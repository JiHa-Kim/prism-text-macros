{
  "name": "prism-text-macros",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.36",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "globals": "^17.2.0",
    "typescript-eslint": "^8.54.0",
    "vitest": "^4.0.18"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "esbuild src/background/background.ts src/content/content.ts src/options/options.ts src/content/pageBridge.ts --bundle --outdir=dist --format=esm --target=esnext",
    "dev": "esbuild src/background/background.ts src/content/content.ts src/options/options.ts src/content/pageBridge.ts --bundle --outdir=dist --format=esm --target=esnext --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest run --watch"
  }
}
