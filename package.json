{
  "name": "prism-text-macros",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.36",
    "esbuild": "^0.27.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "esbuild src/background.ts src/content.ts src/options.ts src/pageBridge.ts --bundle --outdir=dist --format=esm --target=esnext"
  }
}
